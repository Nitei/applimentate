/*
* ¿Por qué le cambiaste el nombre a la Interface? ¿No te gustaba? Esto se ha cargado todos los tests
* ya que no se ejecutan y aparte implica regresionar allá donde se utilizó
* */
export interface AllergenInterface {
    name: string;
    icon: string;
}
